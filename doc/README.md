```

intro

nest_ = {}

_group = {}

_grid = {
    value = {
        v
        a
        en
        order
        init
        x
        y
        lvl
        count
        slew
        draw_slew
    },
    momentary = {
        v
        a
        en
        order
        init
        x
        y
        lvl
        count
    },
    toggle = {
        v
        a
        en
        order
        init
        x
        y
        lvl
        count
    },
    range = { --no input
    },
    fader = { --output 2 == range
    },
    crossfader = { -- output 2 == range
    },
    control = {
        v
        a
        en
        order
        init
        x
        y
        lvl
        input
        output
    },
    metacontrol = {
        v
        a
        en
        order
        init
        x
        y
        lvl
        targets
    }
}

_ arc = {
    value = {
    },
    range = {
    },
    fader = {
    },
    crossfader = {
    }
}

control = {
    v
    a
    en
    order
    init
    inputs
    outputs
}

input = {}
output = {}

```
